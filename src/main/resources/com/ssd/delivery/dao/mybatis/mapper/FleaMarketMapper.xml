<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssd.delivery.dao.mybatis.mapper.FleaMarketMapper">
  <cache />

   <insert id="insert" parameterType="FleaMarketDTO">
      insert into fleamarket
      (fleamarketid, username, fleamarketdate, address, totalprice, itemid)
      values
      (#{fleamarketId}, #{username}, #{fleamarketDate}, #{address}, #{totalPrice}, #{itemId} )
	</insert>

	<update id="update" parameterType="FleaMarketDTO">
    UPDATE FAVORITEUSER SET 
	   FAVORITEUSERNAME  = #{favoriteUsername}, 
	   TRADECOUNT = #{tradeCount} 
    WHERE USERNAME= #{username}
    
    update fleamarket 
    set address = #{address},
    	totalPrice = #{totalPrice}
    where fleamarketid = #{fleaMarketId}"
    
	</update>

	<delete id="delete" parameterType="java.lang.String">
	DELETE FROM fleamarket 
	WHERE fleamarketid = #{fleaMarketId}
	</delete>
	
  
	
	<select id="getFleaMarketById" resultType="FleaMarketDTO">
    select * from fleamarket
    where username = #{username}
	</select>
	
	<select id="getFleaMarketByUsername" resultType="FleaMarketDTO">
   	select * from fleamarket
    where username = #{username}
	</select>
	
	<select id="getFleaMarketList" resultType="FleaMarketDTO">
    select * from fleamarket
	</select>
	
  
</mapper>