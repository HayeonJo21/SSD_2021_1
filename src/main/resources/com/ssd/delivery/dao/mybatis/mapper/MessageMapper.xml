<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssd.delivery.dao.mybatis.mapper.MessageMapper">
  <cache />

   <insert id="insertMessage" parameterType="MessageDTO">
    INSERT INTO MESSAGE
      (USERNAME, CONTENT, MESSAGEDATE, RECEIVERUSERNAME)
    VALUES
      (#{senderUsername}, #{content}, #{messageDate}, #{receiverUsername})
	</insert>

	<update id="updateMessage" parameterType="MessageDTO">
    UPDATE MESSAGE SET
      CONTENT = #{languagePreference}, 
      MESSAGEDATE = #{favouriteCategoryId}, 
      receiverUsername = #{receiverUsername}, 
    WHERE USERNAME = #{username}
	</update>

	<delete id="deleteMessage" parameterType="int">
	DELETE FROM MESSAGE 
	WHERE USERNAME = #{username}
	</delete>
	
	<select id="getMessageListByUsername" resultType="MessageDTO">
    SELECT *
    FROM MESSAGE 
    WHERE USERNAME = #{username}
	</select>
	
	<select id="getMessageContentByUsername" resultType="MessageDTO">
    SELECT CONTENT, MESSGAEDATE
    FROM MESSAGE 
    WHERE USERNAME = #{username} AND RECEIVERUSERNAME = #{receiverUsername}
	</select>
  
	<select id="getMessageList" resultType="MessageDTO">
    SELECT * FROM Message
	</select>
  
</mapper>